// Checkout steps
const STEPS = {
  CART: '#/cart',
  PROFILE: '#/profile',
  SHIPPING: '#/shipping',
  PAYMENT: '#/payment'
};

// OrderForm timeout
const ORDERFORM_TIMEOUT = 500;

const RICA_APP = 'ricafields';

export {
  STEPS,
  ORDERFORM_TIMEOUT,
  RICA_APP
};
