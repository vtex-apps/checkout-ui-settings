// Checkout steps
const STEPS = {
  CART: '#/cart',
  PROFILE: '#/profile',
  SHIPPING: '#/shipping',
  PAYMENT: '#/payment'
};

// TIMEOUT
const TIMEOUT_500 = 500;
const TIMEOUT_750 = 750;

const RICA_APP = 'ricafields';

export {
  STEPS,
  TIMEOUT_500,
  TIMEOUT_750,
  RICA_APP
};
